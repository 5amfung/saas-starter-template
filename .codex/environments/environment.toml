# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "Worktree Environment"

[setup]
script = '''
[ -f "$CODEX_SOURCE_TREE_PATH/apps/web/.env" ] && cp "$CODEX_SOURCE_TREE_PATH/apps/web/.env" "$CODEX_WORKTREE_PATH/apps/web/"

[ -f "$CODEX_SOURCE_TREE_PATH/apps/admin/.env" ] && cp "$CODEX_SOURCE_TREE_PATH/apps/admin/.env" "$CODEX_WORKTREE_PATH/apps/admin/"

[ -f "$CODEX_SOURCE_TREE_PATH/packages/db-schema/.env" ] && cp "$CODEX_SOURCE_TREE_PATH/packages/db-schema/.env" "$CODEX_WORKTREE_PATH/packages/db-schema/"

cd "$CODEX_WORKTREE_PATH"
pnpm install
'''
